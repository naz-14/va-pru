scalar Upload

type FileUpload {
  url: String!
}

type Mutation {
  uploadFile(file: Upload!): FileUpload!
  uploadMultipleFile(file: [Upload!]!): [FileUpload]!
  uploadImage(file: Upload!): FileUpload!
  uploadMultipleImage(file: [Upload!]!): [FileUpload]!
}
