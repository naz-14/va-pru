type SapListPrices {
  id: Int!
  list_number: Int
  list_name: String
  base_number: Int
  round_system: String
}

type Query {
  getSapListPrices(
    limit: Int
    offset: Int
  ): [SapListPrices!]!
}