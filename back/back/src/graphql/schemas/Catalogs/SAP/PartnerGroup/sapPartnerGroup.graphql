type SapPartnerGroup {
  id: Int!
  group_code: Int
  group_name: String
  group_type: String
  price_list: Int
}

type Query {
  getSapPartnerGroup(
    limit: Int
    offset: Int
  ): [SapPartnerGroup!]!
}