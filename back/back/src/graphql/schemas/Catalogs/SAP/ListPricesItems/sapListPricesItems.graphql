type SapListPricesItems {
  id: Int!
  item_code: String
  price_list: Int
  price: Int
  factor: Int
  base_num: Int
}

type Query {
  getSapListPricesItems(
    limit: Int
    offset: Int
  ): [SapListPricesItems!]!
}