type Query {
  getBillingOrders(
    searchQuery: String
    limit: Int
    offset: Int
    platform: Int
  ): OrderRowsCount
}

type Mutation {
  changeToBilling(order_id: Int!, shipping_compay_id: Int!): Order
}