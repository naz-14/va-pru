type Query{
    getToStockOrders(
        searchQuery: String
        limit: Int
        offset: Int
        platform: Int
    ): OrderRowsCount
}

type Mutation {
  changeToSupply(order_id: Int!): Order
}